//Componente que criará a página home para ser renderizada dentro do router, switch, route path
//Todo component é uma função, e dentro da função aceita o formato ejx parecido com o html

function Home(){
  return(
    <section>
      <h1>Home</h1>
    </section>
  )
}

export default Home